{% set data = paragraph %}

{{ drupal_view(data.field_vista_puestos.entity.id, 'puestos') }}
{{ drupal_view(data.field_resultados_reserva.entity.id, 'reservas') }}

<!-- Modal -->
<div id="modal" class="modal">
	<div class="modal-content">
		<span class="close-btn">&times;</span>
		<img id="modal-image" src="" alt="Imagen del modal"/>
		<h2 id="modal-title"></h2>
		<p id="modal-info"></p>
		{{  drupal_entity('webform', data.field_formulario.entity.id) }}
	</div>
</div>
